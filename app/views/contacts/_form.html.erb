<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-sm-4">
      <%= simple_form_for @contact do |f| %>
        <%= f.input :title %>
        <%= f.input :name %>
        <%= f.input :phone %>
        <%= f.input :address %>
        <div class="d-flex justify-content-center">
          <%= f.button :submit, class: "btn btn-add" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
