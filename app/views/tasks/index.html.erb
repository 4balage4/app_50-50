<h2>your households tasks</h2>

<% @tasks.each do |task| %>
  <p><%= link_to task.name, task_path(task) %> <%= task.household.name%> <%= task.assigned_to.nil? ? "unassigned" : task.assigned_to.username %> </p>
<% end %>

<%= link_to "Add new", new_task_path %>
